import { AgentsListView } from "@/components/agents/agents-list";

// The agency's list of agents. The body is shared with the client portal (components/agents).
export default function AgentsPage() {
  return <AgentsListView />;
}
