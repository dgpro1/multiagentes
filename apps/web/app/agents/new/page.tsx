import { AgentWizardView } from "@/components/agents/agent-wizard";

// The agency's create-agent wizard. The body is shared with the client portal (components/agents).
export default function NewAgentPage() {
  return <AgentWizardView />;
}
