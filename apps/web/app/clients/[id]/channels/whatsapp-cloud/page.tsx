import { WhatsAppCloudChannelView } from "@/components/channels/whatsapp-cloud-channel";

// The agency's WhatsApp API numbers of a client. The body is shared with the client portal (components/channels).
export default function WhatsAppCloudChannelPage() {
  return <WhatsAppCloudChannelView />;
}
